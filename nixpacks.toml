[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = [
  "pnpm --filter @store/db run db:generate",
  "pnpm --filter @store/api run build"
]

[start]
cmd = "pnpm --filter @store/db exec prisma migrate deploy && node apps/api/dist/main"
